# /jint:preview

Launch a local preview of the current project.

## Usage

```
/jint:preview
```

## What it does

1. Starts the Next.js dev server if not already running
2. Opens the browser at `http://localhost:3000/edit/<current-project-id>`
3. Reports the URL for Edit mode and View mode
