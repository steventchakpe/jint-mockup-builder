import { PageShell } from '@/components/structural/PageShell';

/** Aperçu de la nouvelle shell reconstruite depuis Figma (carte flottante + UEX re-thémée). */
export default function ShellDemo() {
  return (
    <PageShell>
      <div className="min-h-[600px] p-lg" />
    </PageShell>
  );
}
